{
  "enabled_groups": {
    "description": "QQ groups where automatic join verification is enabled",
    "type": "list",
    "default": [],
    "hint": "Only requests for these groups are processed."
  },
  "platform_id": {
    "description": "AstrBot OneBot platform ID",
    "type": "string",
    "default": "napcat"
  },
  "verifier_base_url": {
    "description": "Identity verification service URL",
    "type": "string",
    "default": "",
    "hint": "Required. Configure this privately in AstrBot; do not commit the endpoint to the repository."
  },
  "verifier_username": {
    "description": "Verification service username",
    "type": "string",
    "default": ""
  },
  "verifier_password": {
    "description": "Verification service password",
    "type": "string",
    "default": "",
    "hint": "Stored only in AstrBot data/config; never commit credentials into the plugin repository."
  },
  "dry_run": {
    "description": "Initial bootstrap mode only; runtime approval is controlled by /njuverify enable|disable",
    "type": "bool",
    "default": true,
    "hint": "Only used to initialize runtime state on first launch. After that, /njuverify status is authoritative."
  },
  "scan_interval_seconds": {
    "description": "Pending request reconciliation interval",
    "type": "int",
    "default": 60
  },
  "retry_backoff_seconds": {
    "description": "Retry delay for transient verification failures",
    "type": "int",
    "default": 300
  },
  "failure_notify_user_id": {
    "description": "QQ user ID that receives private messages for definite verification failures",
    "type": "string",
    "default": "",
    "hint": "Only definite verifier failures are reported. Transient network/LLM failures are not sent."
  },
  "min_verify_interval_seconds": {
    "description": "Minimum delay between verification service requests",
    "type": "float",
    "default": 1.5
  },
  "llm_parser_enabled": {
    "description": "Parse every join application with an AstrBot LLM provider",
    "type": "bool",
    "default": true,
    "hint": "The LLM receives only the applicant answer text and returns name + student ID. QQ/group IDs are not sent."
  },
  "llm_parser_provider_id": {
    "description": "AstrBot chat provider used to extract name and student ID from every application",
    "type": "string",
    "default": "deepseek/deepseek-v4-flash"
  },
  "llm_parser_timeout_seconds": {
    "description": "Timeout for one LLM identity parsing call",
    "type": "int",
    "default": 20
  },
  "llm_parser_max_tokens": {
    "description": "Completion-token budget for the identity parser",
    "type": "int",
    "default": 256,
    "hint": "DeepSeek V4 may spend part of this budget on reasoning before emitting the JSON result."
  }
}
